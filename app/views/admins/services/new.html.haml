.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", admins_services_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_admins_service_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Service
    .inner
      = form_for :service, :url => admins_services_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}

