.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", admins_users_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_admins_user_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      User
    .inner
      = form_for :user, :url => admins_users_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f, :user =>@user}
- content_for :sidebar, render(:partial => 'sidebar')
